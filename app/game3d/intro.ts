/**
 * Reading time for each in-engine story shot.
 *
 * Keep the React card timer and Three.js camera move on this single value so
 * the image, copy, progress indicator, and sound cue cannot drift apart.
 */
export const INTRO_CARD_DURATION_MS = 7000;
export const INTRO_CARD_DURATION_SECONDS = INTRO_CARD_DURATION_MS / 1000;
